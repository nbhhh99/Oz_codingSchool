DB_HOST=localhost
DB_PORT=3306
DB_USER=ozcoding
DB_PASSWORD=pw1234
DB_ROOT_PASSWORD=Password123@!
DB_NAME=ai_health

# 폐렴 예측 작업 큐 / 결과 Pub-Sub 용 Redis.
# 로컬(도커 밖) 실행 시 localhost, docker compose 에서는 compose 가 redis://redis:6379/0 로 덮어쓴다.
REDIS_URL=redis://localhost:6379/0
