# 테스트 전용 의존성 (메인 앱 pyproject.toml 과 분리)
#   uv pip install -r tests/requirements.txt -r worker/requirements.txt
#   uv run pytest            (또는  .venv/Scripts/python -m pytest)
#
# 통합 테스트는 인메모리 SQLite 로 스키마를 만들어 검증하며, MySQL 이 필요 없다.
# worker/requirements.txt(torch 등)도 함께 설치해야 test_ai_prediction_unit 의
# worker 연동 검증이 통과한다.

pytest>=8.0
pytest-asyncio>=0.23
aiosqlite>=0.20
httpx>=0.27
